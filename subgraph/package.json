{
  "name": "@totalreclaw/subgraph",
  "version": "0.1.0",
  "description": "TotalReclaw subgraph — indexes EventfulDataEdge events on Base L2",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 totalreclaw/subgraph",
    "create:local": "graph create --node http://localhost:8020/ totalreclaw/subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0",
    "assemblyscript": "^0.27.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
