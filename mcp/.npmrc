legacy-peer-deps=true
# Harden against transient registry/CDN blips (api.nuget.org onnxruntime
# fetch hung CI repeatedly — see .github/workflows/ci.yml). Retry instead
# of hanging the whole job.
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
